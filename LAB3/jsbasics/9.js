// Convert an array of strings to uppercase using map().

const stringArray = ["hello", "world", "javascript", "programming"];
const newArr = stringArray.map((str) => str.toUpperCase());

console.log("Original Array: ", stringArray);
console.log("New Array: ", newArr);